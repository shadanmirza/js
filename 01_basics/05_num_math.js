const score = 400
// console.log(score);

const balance = new Number(100)
// console.log(balance);

// console.log(balance.toString().length);
// console.log(balance.toFixed(4)); // to fix the value after the point

const otherNumber = 200.8966

// console.log(otherNumber.toPrecision(3));  // to round off the value

const hundreds = 1000000
// console.log(hundreds.toLocaleString('en-IN'));

// +++++++++++++ Maths +++++++++++++++++++++++++++++

// console.log(Math);

// console.log(Math.abs(-4)); // to turn the value in positive

// console.log(Math.round(4.6)); // to round off the value

// console.log(Math.ceil(4.2)); // upper round off the value if only 0.1 extra in the value 

// console.log(Math.floor(4.9)); // lower round off the value if only 0.1 less in the value

// console.log(Math.min(4, 3, 6, 8)); // to find minimun num

// console.log(Math.max(4, 3, 6, 8)); //  to find minimun num

// console.log(Math.random()); // it gives the random value in between 0 to 1

// console.log((Math.random()*10) + 1);
// console.log(Math.floor(Math.random()*10) + 1);

const min = 10
const max = 20

//  console.log(Math.floor(Math.random() * (max - min + 1)) + min) // formula to fix range